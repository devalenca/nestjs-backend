version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nestjs-backend-app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    # Descomentar se quiser usar durante desenvolvimento
    # volumes:
    #   - .:/app
    #   - /app/node_modules
    # command: npm run start:dev
    restart: unless-stopped

  # Opcional: Se você usar um banco de dados como PostgreSQL ou MySQL
  # db:
  #   image: postgres:16-alpine
  #   container_name: nestjs-backend-db
  #   environment:
  #     POSTGRES_DB: nestjs_db
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db-data:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   db-data:
